# chat

a simple Discord style chat app built with HTML, CSS, and JavaScript using Supabase for database and realtime features.

supports servers, channels, direct messages, friends, typing indicators and more

## run locally

You can just run the html locally and the API is `supabase.co` so its unblocked at school :)
